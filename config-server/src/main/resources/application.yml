server:
  port: 8888

spring:
  application:
    name: config-server
  profiles:
    active: dev, native

eureka:
  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/

management:
  endpoints:
    web:
      exposure:
        include: health,info,env

logging:
  level:
    root: INFO
